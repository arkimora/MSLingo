<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/Theme.MSLingo.TWA"
    tools:targetApi="31">

    <meta-data
      android:name="asset_statements"
      android:resource="@string/asset_statements" />

    <activity
      android:name="com.google.androidbrowserhelper.trustedwebactivityui.BasicActivity"
      android:exported="true"
      android:theme="@style/Theme.MSLingo.TWA">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

      <!--
        Edit android:host to include all hosts that the app intends to handle.
        The default here is the production host. Adjust before publishing.
      -->
      <meta-data
        android:name="android.support.customtabs.trustedwebactivity.target"
        android:value="https://mslingo.vercel.app" />
    </activity>
  </application>
</manifest>
