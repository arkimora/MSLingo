## What does this PR do?

<!-- Brief description -->

## Source

<!-- If changing content, link the mnsl.mn URL. If changing code, link the issue. -->

## Checklist

- [ ] `npm run typecheck` passes
- [ ] `npm run test` passes
- [ ] `npm run build` succeeds
- [ ] If content was changed: I only added/removed data that's actually on mnsl.mn
- [ ] If UI was changed: the ink/brass/parchment tokens are used (no `primary-*` or `sand-*` classes)
